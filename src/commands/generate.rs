// generateコマンドの基本処理
pub fn run(dry_run: bool, force: bool, watch: bool) {
    println!(
        "[generate] dry_run: {}, force: {}, watch: {}",
        dry_run, force, watch
    );
    // TODO: 実装
}
